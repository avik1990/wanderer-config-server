
server:
  port: 9097

spring:
  application:
    name: file-service
  config:
    import: optional:configserver:http://config-server:8088 

  datasource:
    url: jdbc:postgresql://sr-postgres:5432/file_service
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver

  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerialize
eureka:
  client:
    service-url:
      defaultZone: http://service-registry:8761/eureka/

